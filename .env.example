# ---------- SERVER ----------
# For local dev, Prisma uses SQLite by default (see prisma/schema.prisma).
# To use Postgres (e.g., on DigitalOcean), set DATABASE_URL here and switch the
# provider in prisma/schema.prisma to "postgresql".
DATABASE_URL="file:./dev.db"
PORT=4000
CORS_ORIGIN=http://localhost:5173

# ---------- CLIENT ----------
VITE_API_URL=http://localhost:4000/api
